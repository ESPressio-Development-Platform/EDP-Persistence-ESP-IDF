# Compiler Definitions and Conditional Compilation

No repository-defined compiler definitions were found in the audited PlatformIO/CMake/SDK configuration. Binding profiles and NVS/VFS provider semantics are C++ contract choices rather than EDP preprocessor switches.

ESP-IDF configuration symbols supplied by the SDK/application are external platform configuration unless this repository explicitly defines them.

> Audited against `d359e06051513d638b5ce54cf071e91076dbf380` on `main` across 4 build-configuration file(s).
