# Internal API

Internal adapters map VFS/NVS return codes to EDP status/fact results, build bounded paths/keys, perform scratch-buffer copying through ByteOperations and manage provider Open/Close state where required.
