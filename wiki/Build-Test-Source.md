# Build, Test and Source Map

C++20 and ESP-IDF are required. `docs/PROVIDERS.MD` and `docs/BUILDING.MD` are the durable provider/build references. Tests cover representability, truncation, result normalization, binding-profile metadata and concrete provider behaviour.
